import { FitroCompletadoPipe } from './fitro-completado.pipe';

describe('FitroCompletadoPipe', () => {
  it('create an instance', () => {
    const pipe = new FitroCompletadoPipe();
    expect(pipe).toBeTruthy();
  });
});
